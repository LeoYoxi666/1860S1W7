CHIP DecoderSegment201801673 {
    IN A, B, C, D;
    OUT c;

    PARTS:
    Not(in=A, out=notA);
    Not(in=B, out=notB);
    Xor(a=C, b=D, out=CxorD);
    Not(in=CxorD, out=notCxorD);

    Or(a=notA, b=notB, out=AorB);
    Or(a=AorB, b=notCxorD, out=c);
}
